name: Build and Test

on:
  push:

jobs:
  compute-version:
    uses: cpprentice/ci-templates/.github/workflows/compute-python-package-version.yaml@main
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Install Build
        run: python -m pip install build

      - name: Build
        run: |
          python -m build

      - name: Upload build artifacts
        uses: actions/upload-artifact@v7
        with:
          name: python-dist
          path: dist/


  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Cache pip
        uses: actions/cache@v5
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements*.txt', '**/pyproject.toml', '**/poetry.lock') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e .[test]

      - name: Run tests with coverage
        run: coverage run --source=src -m pytest test
      - name: Create XML Coverage report (fail under 75%)
        run: coverage xml --fail-under=75

      - name: Upload coverage report (XML)
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: coverage-xml
          path: coverage.xml

  publish:
    name: Publish package on pypi
    runs-on: ubuntu-latest
    needs:
      - compute-version
      - build
      - test
    # if: ${{ !contains(needs.compute-version.outputs.version, 'dev') }}
    environment:
      name: pypi
      url: https://pypi.org/p/jsonpatch-plus
    permissions:
      id-token: write
    steps:
      - name: Print version to publish
        run: echo "Publishing version ${{ needs.compute-version.outputs.version }}"
      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          name: python-dist
          path: dist/
      - name: Print contents of dist
        run: ls -la dist
      - name: Ensure version is non-dev
        if: ${{ contains(needs.compute-version.outputs.version, 'dev') }}
        run: |
          echo "Refusing to publish a dev version!"
          exit 1
      - name: Publish package distributions to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
